# vite dev 에서만 로드된다 (npm run build 에는 적용되지 않는다).
# /api 로 두면 vite dev 프록시(-> http://127.0.0.1:8000)를 탄다 (vite.config.ts).
VITE_API_BASE_URL=/api

# 실제 백엔드에 붙는다. 별도 터미널에서 `uvicorn main:app --reload` 로 띄워 둘 것.
# 백엔드 없이 화면만 보려면 true 로 바꾼다 (데모 계정 demo@example.com / demo1234).
VITE_USE_MOCK=false
